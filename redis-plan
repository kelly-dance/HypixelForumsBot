posts -> set[post ids]

categories -> set[tags]
category:{tag} -> hash {name, url, ???}

subs:{tag} -> set[hook ids]

hook:{id} -> hash {id, token, guild, ???}
hook:{id}:subs -> set[tags]

guild:{id}:hooks -> set[hook ids]

use: 
sudo docker-compose run redis redis-cli -h redis
